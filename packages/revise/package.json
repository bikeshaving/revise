{
  "name": "@createx/revise",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "clean": "shx rm -rf ./lib",
    "prebuild": "yarn clean",
    "build": "tsc --sourceMap",
    "watch": "yarn run build -w",
    "develop": "yarn test --watchAll",
    "test": "jest",
    "lint": "eslint --ext js,ts src"
  },
  "dependencies": {
    "@channel/limiters": "^0.1.0",
    "@channel/pubsub": "^0.1.0"
  }
}
