{
  "name": "@createx/revise",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "clean": "shx rm -rf ./lib",
    "prebuild": "yarn clean",
    "build": "tsc --sourceMap",
    "watch": "yarn run build -w",
    "develop": "yarn test --watch",
    "test": "jest",
    "lint": "eslint --ext js,ts src"
  },
  "dependencies": {
    "@channel/channel": "^1.0.0",
    "@channel/limiters": "^0.2.1",
    "@channel/pubsub": "^0.2.1"
  }
}
