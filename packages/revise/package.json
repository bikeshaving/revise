{
  "name": "@createx/revise",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "clean": "shx rm -rf ./lib",
    "prebuild": "yarn clean",
    "build": "tsc --sourceMap",
    "watch": "yarn run build -w",
    "develop": "yarn test --watch",
    "test": "jest",
    "lint": "eslint --ext js,ts src"
  },
  "dependencies": {
    "@repeaterjs/limiters": "^0.3.1",
    "@repeaterjs/pubsub": "^0.3.2",
    "@repeaterjs/repeater": "^2.0.0"
  },
  "devDependencies": {
    "ws": "^7.0.0"
  }
}
