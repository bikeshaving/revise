{
  "name": "@createx/revise-knex",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "clean": "shx rm -rf ./lib",
    "prebuild": "yarn clean",
    "build": "tsc --sourceMap",
    "watch": "yarn run build -w",
    "develop": "yarn test --watch",
    "test": "jest",
    "lint": "eslint --ext js,ts src"
  },
  "dependencies": {
    "@channel/pubsub": "^0.2.1",
    "@createx/revise": "*"
  },
  "devDependencies": {
    "@channel/timers": "^0.2.1",
    "dotenv": "^8.0.0",
    "knex": "^0.17.5",
    "pg": "^7.11.0"
  },
  "peerDependencies": {
    "knex": "^0.16.5"
  }
}
